Make small, clear commits
Each commit should do one thing and have a descriptive message (e.g., “add login validation”).

Use branches
Create separate branches for features or fixes instead of working directly on main.

Review before merging
Use pull requests to review code, catch mistakes, and keep the codebase stable.